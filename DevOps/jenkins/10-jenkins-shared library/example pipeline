Example Pipeline:
-----------------
    @Library('shared-lib') _
    pipeline {
        agent any
            stages {
                stage('Clean') {
                    steps {
                        build('Clean')
                    }
                stage('Build') {
                    steps {
                        build('build')
                    }
                }

            }
    }